name: smt_sensor
description: Flutter package for USB serial communication with K3_Pro_Spy sensor
version: 0.0.1
homepage: 

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  usb_serial: ^0.5.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
